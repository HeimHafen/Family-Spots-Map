name: Family Spots Map CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    name: Code Quality Checks
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🔧 Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: 📦 Install Linters
        run: npm install -g htmlhint csslint eslint

      - name: 🔍 Run HTML Linter
        run: |
          if [ -f htmlhint.json ]; then
            echo "→ Running HTMLHint..."
            htmlhint .
          else
            echo "⚠️ htmlhint.json nicht gefunden – HTML-Skip"
          fi

      - name: 🎨 Run CSS Linter
        run: |
          if [ -f .csslintrc ]; then
            echo "→ Running CSSLint..."
            csslint css/
          else
            echo "⚠️ .csslintrc nicht gefunden – CSS-Skip"
          fi

      - name: 💻 Run JavaScript Linter
        run: |
          if [ -f .eslintrc.json ]; then
            echo "→ Running ESLint..."
            eslint js/
          else
            echo "⚠️ .eslintrc.json nicht gefunden – JS-Skip"
          fi

      - name: ✅ Linting abgeschlossen
        run: echo "✨ Alle statischen Codeprüfungen abgeschlossen!"