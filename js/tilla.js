// js/tilla.js
// Mindest-Version, damit die App nicht kaputt geht.
// Wir füllen das später mit dem schönen Tilla-Layout.

export function initTilla() {
  // Erstmal nur ein Log – so sehen wir im Dev-Tool, dass es läuft.
  console.log("Tilla initTilla() wurde aufgerufen");
}

export function showTillaMessage(_text) {
  // Platzhalter – macht noch nichts, verhindert aber Fehlermeldungen.
}